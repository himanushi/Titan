expression = additive

additive = multitive ((plus / minus) multitive)*
plus = { "+" }
minus = { "-" }

multitive = primary ((times / divide) primary)*
times = { "*" }
divide = { "/" }

primary = number / ("(" _ additive _ ")")
number = { (["0".."9"])+ }
_ = _{ " " | "\n" }
