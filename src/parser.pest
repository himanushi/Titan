expression = { __ ~ number ~ __ }

SPACE = _{ " " }
__ = _{ SPACE* }

// 四則演算
add = { "+" }
sub = { "-" }
mult = { "*" }
div = { "/" }

// 数字型
number = { integer }
integer = { "0" | "-"? ~ ('1'..'9') ~ ('0'..'9')* }
